# Data Directory

Local OpenAlex data is written here by the pipeline and ignored by Git.

- `raw/`: raw API downloads when needed
- `interim/`: taxonomy, counts, plans, and validation summaries
- `processed/`: cleaned text corpus

Only `.gitkeep` placeholders are tracked.
